<%= form_tag word_search_path, :method => 'get' do %>
	<%= label_tag(:word, "Search for") %>
	<%= text_field_tag(:word) %>
	<%= submit_tag("Search") %>
<% end %> 

<% if !@words.blank? %>
  <% for word in @words %>
  <% end %>
<% else %>
  
<% end %>